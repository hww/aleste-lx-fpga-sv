# config.yaml
serial:
  port: "/dev/ttyUSB4"  # или "COM3" на Windows
  baudrate: 115200
  timeout: 1.0          # сек

protocol:
#  start_byte: 0xA5      # Стартовый байт пакета
#  max_packet_size: 1024

