; ============================================================================
; AUDIO DRIVER - Заглушки
; ============================================================================
module audio

; ----------------------------------------------------------------------------
; audio_init - Инициализация аудио системы
; Вход: Нет
; Выход: A = код ошибки (0=успех)
; ----------------------------------------------------------------------------
PUBLIC _audio_init
_audio_init:
audio_init_:
    xor a  ; Заглушка: успех
    ret

; ----------------------------------------------------------------------------
; audio_play - Воспроизвести звук
; Вход: HL = указатель на аудио данные
;       DE = длина данных
;       B = частота дискретизации
;       C = каналы (1=моно, 2=стерео)
; Выход: A = код ошибки
; ----------------------------------------------------------------------------
PUBLIC _audio_play
_audio_play:
audio_play_:
    xor a  ; Заглушка: успех
    ret

; ----------------------------------------------------------------------------
; audio_stop - Остановить воспроизведение
; Вход: Нет
; Выход: Нет
; ----------------------------------------------------------------------------
PUBLIC _audio_stop
_audio_stop:
audio_stop_:
    ; TODO: Реализовать остановку
    ret

; ----------------------------------------------------------------------------
; audio_set_volume - Установить громкость
; Вход: A = громкость (0-255)
;       B = канал (0=все, 1=левый, 2=правый)
; Выход: Нет
; ----------------------------------------------------------------------------
PUBLIC _audio_set_volume
_audio_set_volume:
audio_set_volume_:
    ; TODO: Реализовать установку громкости
    ret

; ----------------------------------------------------------------------------
; audio_get_status - Получить статус аудио системы
; Выход: A = статус (бит 0=играет, бит 1=пауза, бит 7=ошибка)
; ----------------------------------------------------------------------------
PUBLIC _audio_get_status
_audio_get_status:
audio_get_status_:
    xor a  ; Заглушка: ничего не играет
    ret