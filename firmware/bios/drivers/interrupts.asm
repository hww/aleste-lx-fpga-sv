; ============================================================================
; INTERRUPTS AND TIMERS - Заглушки
; ============================================================================
module interrupts

; ----------------------------------------------------------------------------
; int_enable - Разрешить прерывания
; Вход: Нет
; Выход: Нет
; ----------------------------------------------------------------------------
PUBLIC _int_enable
_int_enable:
int_enable_:
    ei
    ret

; ----------------------------------------------------------------------------
; int_disable - Запретить прерывания
; Вход: Нет
; Выход: Нет
; ----------------------------------------------------------------------------
PUBLIC _int_disable
_int_disable:
int_disable_:
    di
    ret

; ----------------------------------------------------------------------------
; int_set_handler - Установить обработчик прерывания
; Вход: A = номер прерывания (0-7)
;       HL = адрес обработчика
; Выход: Нет
; ----------------------------------------------------------------------------
PUBLIC _int_set_handler
_int_set_handler:
int_set_handler_:
    ; TODO: Реализовать установку вектора прерывания
    ret

; ----------------------------------------------------------------------------
; timer_start - Запустить таймер
; Вход: A = номер таймера (0-3)
;       HL = период в тиках
; Выход: Нет
; ----------------------------------------------------------------------------
PUBLIC _timer_start
_timer_start:
timer_start_:
    ; TODO: Реализовать запуск таймера
    ret

; ----------------------------------------------------------------------------
; timer_stop - Остановить таймер
; Вход: A = номер таймера
; Выход: Нет
; ----------------------------------------------------------------------------
PUBLIC _timer_stop
_timer_stop:
timer_stop_:
    ; TODO: Реализовать остановку таймера
    ret

; ----------------------------------------------------------------------------
; timer_get_count - Получить текущее значение таймера
; Вход: A = номер таймера
; Выход: HL = значение счетчика
; ----------------------------------------------------------------------------
PUBLIC _timer_get_count
_timer_get_count:
timer_get_count_:
    ld hl, 0  ; Заглушка
    ret

; ----------------------------------------------------------------------------
; get_tick_count - Получить счетчик тиков системы
; Выход: HL = счетчик тиков (младшее слово)
;        DE = счетчик тиков (старшее слово)
; ----------------------------------------------------------------------------
PUBLIC _get_tick_count
_get_tick_count:
get_tick_count_:
    ld hl, 0
    ld de, 0  ; Заглушка
    ret

; ----------------------------------------------------------------------------
; delay_ms - Задержка в миллисекундах
; Вход: HL = количество миллисекунд
; Выход: Нет
; ----------------------------------------------------------------------------
PUBLIC _delay_ms
_delay_ms:
delay_ms_:
    ; TODO: Реализовать точную задержку
    ret